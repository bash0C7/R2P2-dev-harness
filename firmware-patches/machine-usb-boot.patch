diff --git a/mrbgems/picoruby-machine/include/machine.h b/mrbgems/picoruby-machine/include/machine.h
index 35a4d069..9b80f97d 100644
--- a/mrbgems/picoruby-machine/include/machine.h
+++ b/mrbgems/picoruby-machine/include/machine.h
@@ -66,6 +66,7 @@ bool Machine_set_hwclock(const struct timespec *ts);
 bool Machine_get_hwclock(struct timespec *ts);
 void Machine_exit(int status);
 void Machine_reboot(void);
+void Machine_usb_boot(void);
 uint64_t Machine_uptime_us(void);
 
 #define MACHINE_EXIT_REBOOT 120
diff --git a/mrbgems/picoruby-machine/ports/rp2040/machine.c b/mrbgems/picoruby-machine/ports/rp2040/machine.c
index dc8eb26e..218e4a40 100644
--- a/mrbgems/picoruby-machine/ports/rp2040/machine.c
+++ b/mrbgems/picoruby-machine/ports/rp2040/machine.c
@@ -15,6 +15,7 @@
 #include "hardware/clocks.h"
 #include "hardware/sync.h"
 #include "pico/aon_timer.h"
+#include "pico/bootrom.h"
 #if defined(PICO_RP2350)
 #include "hardware/powman.h"
 #endif
@@ -689,6 +690,12 @@ Machine_bootsel_pressed_q(void)
   return !get_bootsel_button();
 }
 
+void
+Machine_usb_boot(void)
+{
+  rom_reset_usb_boot(0, 0);
+}
+
 
 /*-------------------------------------
  *
diff --git a/mrbgems/picoruby-machine/src/mruby/machine.c b/mrbgems/picoruby-machine/src/mruby/machine.c
index aa3935e7..5e3e835f 100644
--- a/mrbgems/picoruby-machine/src/mruby/machine.c
+++ b/mrbgems/picoruby-machine/src/mruby/machine.c
@@ -493,6 +493,17 @@ mrb_s__reboot(mrb_state *mrb, mrb_value self)
   return mrb_nil_value();
 }
 
+static mrb_value
+mrb_s_usb_boot(mrb_state *mrb, mrb_value self)
+{
+#if defined(PICORB_PLATFORM_RP2)
+  Machine_usb_boot();
+#else
+  mrb_raise(mrb, E_NOTIMP_ERROR, "Machine.usb_boot is not supported on this platform");
+#endif
+  return mrb_nil_value();
+}
+
 void
 mrb_picoruby_machine_gem_init(mrb_state* mrb)
 {
@@ -526,6 +537,7 @@ mrb_picoruby_machine_gem_init(mrb_state* mrb)
 
   mrb_define_class_method_id(mrb, module_Machine, MRB_SYM(exit), mrb_s_exit, MRB_ARGS_OPT(1));
   mrb_define_class_method_id(mrb, module_Machine, MRB_SYM(_reboot), mrb_s__reboot, MRB_ARGS_NONE());
+  mrb_define_class_method(mrb, module_Machine, "usb_boot", mrb_s_usb_boot, MRB_ARGS_NONE());
   mrb_define_class_method_id(mrb, module_Machine, MRB_SYM(check_signal), mrb_machine_check_signal, MRB_ARGS_NONE());
   mrb_define_class_method_id(mrb, module_Machine, MRB_SYM(poll_signal), mrb_machine_poll_signal, MRB_ARGS_NONE());
 
